# Live search. This value is bundled into the browser; use a server-side proxy for production.
VITE_SCRAPINGDOG_API_KEY=

# Optional metadata for the watch page. Restrict this key by origin in Google Cloud.
VITE_YOUTUBE_API_KEY=
